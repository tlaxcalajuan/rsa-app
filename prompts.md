# Prompts de Inteligencia Artificial - rsa-app

Lista de prompts estructurados en formato JSON para el desarrollo y mantenimiento del frontend `rsa-app`.

---

### 1. Componente WelcomeForm
```json
{
  "rol": "Angular Developer",
  "contexto": "Proyecto Angular 20 Standalone",
  "tarea": "Crear componente WelcomeForm para entrada de datos",
  "restricciones": ["Usar Angular Material", "Formulario reactivo o basado en modelos", "Diseño responsivo"],
  "formato": "TS, HTML y SCSS"
}
```

### 2. Reconocimiento de Voz
```json
{
  "rol": "Frontend Engineer",
  "contexto": "Interacción por voz mediante Web Speech API",
  "tarea": "Implementar lógica de captura de voz para el input de nombre",
  "restricciones": ["No usar librerías externas", "Filtrar caracteres no alfanuméricos", "Manejar eventos onstart/onend"],
  "formato": "Método TypeScript"
}
```

### 3. Integración con Backend
```json
{
  "rol": "Angular Developer",
  "contexto": "Consumo de API REST de encriptación",
  "tarea": "Implementar llamada HTTP POST para encriptar el nombre",
  "restricciones": ["Usar HttpClient", "Detección de cambios zoneless", "Manejo de estados de carga"],
  "formato": "Servicio o método de componente"
}
```

### 4. Interfaz y Estilos (UI/UX)
```json
{
  "rol": "UI/UX Designer",
  "contexto": "Estética moderna y premium para aplicación web",
  "tarea": "Diseñar los estilos del formulario con efectos visuales",
  "restricciones": ["Usar Flexbox/Grid", "Gradients y sombras suaves", "Micro-animación en icono de micro"],
  "formato": "SCSS con variables"
}
```

### 5. Pruebas de Calidad (Testing)
```json
{
  "rol": "QA Automation",
  "contexto": "Pruebas unitarias para componentes Angular",
  "tarea": "Escribir suite de pruebas para validación de formulario y mocks de API",
  "restricciones": ["Usar Jasmine y Karma", "Configuración provideZonelessChangeDetection", "Mockear navigator.clipboard"],
  "formato": "Archivo .spec.ts completo"
}
```
